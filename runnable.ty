runnable {
  inputQueue: queue
  outputQueue: queue

  method /react (message)
  method /initially
  method /busy >> boolean
  method /ready >> boolean

  method /popInputQueue >> message

  method [/iterateOutputs] >> message
}